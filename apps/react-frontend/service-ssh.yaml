apiVersion: v1
kind: Service
metadata:
  name: react-frontend-ssh-service
  namespace: react-frontend
  labels:
    app: react-frontend-ssh
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: react-frontend-ssh
