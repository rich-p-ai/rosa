apiVersion: v1
kind: Service
metadata:
  name: nodejs-backend-service
  namespace: nodejs-backend
  labels:
    app: nodejs-backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: nodejs-backend
