apiVersion: v1
kind: Service
metadata:
  name: vue-frontend-ssh-service
  namespace: vue-frontend
  labels:
    app: vue-frontend-ssh
    app.kubernetes.io/name: vue-frontend-ssh
    app.kubernetes.io/part-of: web-applications
  annotations:
    argocd.argoproj.io/sync-wave: "15"
spec:
  type: ClusterIP
  ports:
  - name: ssh
    port: 22
    targetPort: 22
    protocol: TCP
  selector:
    app: vue-frontend-ssh
